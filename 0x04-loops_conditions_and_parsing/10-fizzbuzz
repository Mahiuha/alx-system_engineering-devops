#!/usr/bin/env bash
# Bash script for create a Fizz Buzz
i=1
while [ $i -le 100 ]
do
	if [ $((i % 15)) == 0 ];
	then
		echo "FizzBuzz"

	elif [ $((i % 5)) == 0 ];
	then
		echo "Buzz"

	elif [ $((i % 3)) == 0 ];
	then
		echo "Fizz"
	else
		echo $i
	fi
	((i++))
done
